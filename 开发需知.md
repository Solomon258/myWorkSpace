# 开发需知

> 面向「拿到这份代码、要动手改」的人。
> 只想把工作台用起来，请看 [README.md](README.md)；只想查表结构与字段，请看 [`personal-workbench/docs/开发文档-Java8版.md`](personal-workbench/docs/开发文档-Java8版.md)。

本文档分三层，**越往下越是「不知道就一定会踩」的东西**：

1. 怎么把环境跑起来、代码放在哪、命令是什么
2. 怎么写才符合这个项目的约定（规范、架构、测试）
3. **硬红线** —— 违反它们不会报错，只会静默失效

---

## 目录

- [1. 五分钟跑起来](#1-五分钟跑起来)
- [2. 环境要求](#2-环境要求)
- [3. 工程结构](#3-工程结构)
- [4. 常用命令](#4-常用命令)
- [5. 技术选型与版本锁定](#5-技术选型与版本锁定)
- [6. 架构约定](#6-架构约定)
- [7. 代码规范](#7-代码规范)
- [8. 硬红线（违反会静默失效）](#8-硬红线违反会静默失效)
- [9. 测试与验证](#9-测试与验证)
- [10. 打包与分发](#10-打包与分发)
- [11. 分支模型与提交规范](#11-分支模型与提交规范)
- [12. 提交 Pull Request](#12-提交-pull-request)
- [13. 已知缺口](#13-已知缺口)
- [14. 报告问题](#14-报告问题)

---

## 1. 五分钟跑起来

```bash
git clone https://github.com/Solomon258/myWorkSpace.git
cd myWorkSpace/personal-workbench/server

./mvnw spring-boot:run          # Windows 下用 mvnw.cmd spring-boot:run
# 打开 http://localhost:18080
```

首次进入会跳到 `/setup.html` 走初始化向导（管理员用户名 / 密码 / 时区）。**勾选「加入少量示例数据」**能让你立刻看到各个页面填满的样子，之后可以在设置里一键清除（示例数据都带 `is_demo = 1` 标记）。

不想装 JDK 8 的话，用 Docker 也可以：

```bash
cd personal-workbench
docker compose -f docker-compose.dev.yml up -d --build
docker compose logs -f
```

---

## 2. 环境要求

| 依赖 | 版本要求 | 说明 |
|---|---|---|
| JDK | **必须是 8** | `pom.xml` 中 `maven-enforcer-plugin` 强制校验 `[1.8,1.9)`，用 11/17 会直接构建失败 |
| Maven | 3.8 ~ 3.9 | 也可直接用仓库内置的 `mvnw` / `mvnw.cmd`，无需预装 |
| 数据库 | 无需安装 | SQLite 单文件，Flyway 首次启动自动建库 |
| Node.js | **不需要** | 前端为原生 JS，无构建步骤（仅辅助脚本 `smoke-frontend.js` 需要，可选） |
| Python | 3.8+ | 仅打包与校验脚本用，纯标准库，**不需要 pip install** |

验证环境：

```bash
cd personal-workbench/server
./mvnw -v          # 确认输出 Java version: 1.8.x
```

> **为什么锁死 Java 8**：交付形态是自带 JRE 的免安装包。Java 8 没有 `jlink`（JEP 282 是 Java 9 引入的），只能整包塞 JRE 8 目录再手工裁剪，约 70MB 未压缩。**代价是语言特性受限**（无 `record` / `var` / 文本块 / `switch` 表达式），以及 Spring Boot 只能停在 2.7.18（最后一个支持 Java 8 的主线，开源支持已于 2023-06-30 结束）。
> 换来的是：目标机装的是 Java 8、Java 21 还是没装 Java，都与使用者无关。

---

## 3. 工程结构

```text
personal-workbench/
├── server/                    # 唯一的构建单元（Maven 单模块）
│   ├── src/main/java/com/icecode/workbench/
│   │   ├── auth/              # 登录与首次初始化
│   │   ├── attachment/        # 附件上传与多态绑定
│   │   ├── collect/           # 文章收录（分享页解析）
│   │   ├── common/            # 统一响应体 / 错误码 / 全局异常处理
│   │   ├── config/            # 数据源、WebMvc、工作目录初始化
│   │   ├── dashboard/         # 驾驶舱指标与下钻
│   │   ├── demo/              # 演示数据装载
│   │   ├── favorite/          # 收藏
│   │   ├── inbox/             # 收集箱与 AI 整理（本地规则 + LLM 双通道）
│   │   ├── knowledge/         # 知识库、Obsidian 对接、AI 问答
│   │   ├── poem/              # 两侧诗词与全屏品读
│   │   ├── pomodoro/          # 番茄钟
│   │   ├── schedule/          # 日程（周视图 / 待定时间区 / 重复期次）
│   │   ├── search/            # 全局搜索与语义联想
│   │   ├── settings/          # 设置
│   │   ├── system/            # 备份与恢复
│   │   ├── task/              # 任务与状态机
│   │   ├── timeline/          # 操作时间线
│   │   ├── transfer/          # 跨实体搬运
│   │   ├── trash/             # 回收站
│   │   ├── util/              # 文本与时间工具
│   │   └── vision/            # 图片多模态解析
│   └── src/main/resources/
│       ├── application.yml
│       ├── logback-spring.xml
│       ├── db/migration/      # Flyway V1 - V13（只增不改）
│       └── static/            # 零框架前端
│           ├── index.html     # 单页应用外壳
│           ├── setup.html     # 首次配置 / 登录
│           ├── app.js         # 全部前端逻辑（约 270KB，按视图分块）
│           ├── api.js         # 接口封装，唯一的 fetch 出口
│           ├── style.css      # 全部样式
│           └── poems.js       # 诗词库（500 首，独立文件便于替换）
├── demo/                      # 可交互 HTML 原型 —— 视觉与交互的**事实标准**
├── deploy/                    # 启动 / 停止 / 备份 / 打包
│   ├── build_packages.py      # 唯一打包入口
│   ├── docker/                # 可选的容器跑法
│   └── scripts/               # launcher.ps1（全部启动逻辑）+ 前端冒烟脚本
├── docs/                      # 需求、设计、验收文档（会滞后于代码）
└── scripts/                   # 辅助校验脚本
```

**不入库的目录**：`dev-data/`（真实数据）、`dist/`（打包产物与 JRE）、`logs/`、各层 `output/`（冒烟截图与验证产物）、`server/target/`。

> ⚠️ **`dev-data/workbench.db` 是真实使用数据，永远不要提交**。提交前用 `git status` 复核一遍。

---

## 4. 常用命令

```bash
cd personal-workbench/server

./mvnw spring-boot:run            # 启动开发服务，默认 http://localhost:18080
./mvnw clean test                 # 跑全部测试（26 个测试类，265 个用例）
./mvnw clean package              # 产出 target/workbench.jar
./mvnw -Dtest=TaskFlowTest test   # 只跑某个测试类
```

**端口冲突**：用 `--server.port=` 覆盖，且**必须写在 `-jar` 之后**（写在前面会被 JVM 当成无法识别的选项）。

```bash
java -jar target/workbench.jar --server.port=18081
```

辅助脚本：

```bash
# 扫描 CSS 中「注释夹在选择器中间」导致的规则静默失效
python personal-workbench/scripts/check_css_selectors.py

# 前端冒烟（需先启动服务）
node personal-workbench/deploy/scripts/smoke-frontend.js
```

---

## 5. 技术选型与版本锁定

**以下版本为硬性锁定，升级前需重新验证 Java 8 兼容性。**

| 层次 | 组件 | 版本 | 说明 |
|---|---|---|---|
| 运行时 | JDK | **1.8（8u342+）** | 开发与运行环境 |
| 框架 | Spring Boot | **2.7.18** | 最终 2.7 版 |
| Web | spring-boot-starter-web | 随 Boot | Tomcat 9.0 + `javax.servlet`（**不是** `jakarta.*`） |
| 数据 | spring-boot-starter-jdbc | 随 Boot | JdbcTemplate，**不用 JPA** |
| 数据库 | org.xerial:sqlite-jdbc | **3.41.2.2** | 内置 SQLite 3.41，支持 FTS5、`VACUUM INTO` |
| 迁移 | org.flywaydb:flyway-core | **8.5.13** | **由 Boot 依赖管理自动锁定，不要手动覆盖版本** |
| 校验 | spring-boot-starter-validation | 随 Boot | Hibernate Validator 6.x（`javax.validation`） |
| 密码 | spring-security-crypto | 随 Boot | 只用 `BCryptPasswordEncoder`，**不引整个 Security** |
| HTTP 客户端 | **RestTemplate**（spring-web 自带） | — | Java 8 无 `java.net.http`，不引 OkHttp |
| 前端 | 原生 HTML + CSS + JS | — | **无构建链，无 Node，无框架** |

### 明确不要引入的依赖

| 依赖 | 不要引入的原因 |
|---|---|
| `spring-boot-starter-data-jpa` | Hibernate 5 无官方 SQLiteDialect，引第三方 dialect 是额外风险 |
| `okhttp` / `httpclient5` | Java 8 下需拉 Kotlin stdlib 或额外版本管理；RestTemplate 已够用 |
| 任何 CDN / 外部字体 / 图表库 | **离线可用是硬要求**，任何外链都是 bug |
| 任何前端框架 / 构建工具 | 免安装包内直接静态托管，加构建链等于毁掉 P3 |

---

## 6. 架构约定

### 6.1 分层

```text
Controller  →  Service  →  Repository
   ↑              ↑            ↑
 只做参数校验    业务规则、事务    唯一写 SQL 的地方
 与响应包装      与领域判断
```

- Controller **不写业务逻辑**，不直接碰数据库。
- SQL 全部收敛在 `*Repository`，Service 层只调仓储方法。
- 跨实体、跨表的组合操作放在 Service，用 `@Transactional(rollbackFor = Exception.class)` 包住。

### 6.2 统一响应体

所有接口返回同一个信封（`common/ApiResponse.java`）：

```json
{ "code": 0, "message": "ok", "data": { }, "timestamp": 1789981214972 }
```

`code = 0` 表示成功，非 0 为业务错误码（见 `common/ErrorCode.java`）。

### 6.3 错误处理

- **错误响应必须能指导操作**：`1002 INVALID_PARAMETER` 必须带中文自定义原因；请求 DTO 的校验注解必须自带中文 `message`。
- **客户端错误不得落 500 兜底**：非法 JSON → 400，请求方法不匹配 → 405。
- **同一个约束在不同入口必须使用同一个 ErrorCode**，否则前端没法统一处理。

### 6.4 数据库与事务

- SQLite 开 **WAL + `busy_timeout=5000` + 外键约束**。
- **Hikari 连接池固定 4 个连接**（Windows 上每次新建 SQLite 连接要重建 db/wal/shm 三个句柄并被安全软件扫描，单连接固定约 160ms；池化是为了摊掉这个成本）。
  → 因此**写事务必须短**，**网络 IO 绝不允许包在事务里**（一次 LLM 调用几秒钟，会把 4 个连接全占满）。
- 需要「替换数据库文件」的恢复流程：`allowPoolSuspension=true`，恢复时 `suspendPool()` → `softEvictConnections()` → 重试替换 → `resumePool()`。**少了挂起这一步**，池会在文件替换窗口补建连接、把缺失路径当空库打开，报「no such table」。

### 6.5 静态资源

前端没有构建步骤，`server/src/main/resources/static/` 下的文件就是最终产物。

- 开发时若只改了前端，**把文件复制进 `target/classes/static/`** 即可生效（Spring 对 classpath 静态资源是每次请求现场解析的）。
- ⚠️ **但 Java 类没有这条捷径**。`@RequestMapping` 是应用启动那一刻扫出来的：改了 Controller / Service 后**必须重启服务**，否则新加的路径在运行中的进程里根本不存在，前端拿到 404 而所有单测都是绿的。这是本项目真实踩过的坑。

---

## 7. 代码规范

### 7.1 后端（Java 8）

- **只能使用 Java 8 语法**：不用 `var`、不用 `List.of()`、不用 `Map.of()`、不用文本块。
- 注释写「**为什么**」，不写「做了什么」。凡是「踩过一次才这么写」的地方，把当时的症状记进注释。
- 字段留空要有显式语义，不要隐式兜底（例如「局部更新时 `null` = 不改」和「清空 = 发空串」必须能区分）。
- 新增接口时同步补 `*FlowTest` 集成测试。

### 7.2 前端（原生 JS）

- 不引入任何前端框架与构建工具。
- **任何交互分支都要有 `else`**，不要留「点了没反应」的静默路径（`handleAction` 缺分支是经典故障源）。
- 避免 `$("id")` 撞到不存在的元素后在顶层抛 `TypeError` —— 这会让**整页脚本连同初始化一起静默失效**，表现为整个页面都点不动，而控制台之外毫无线索。
- 前端 `maxlength` 必须与后端 `@Size(max)` 保持一致。
- 样式选择器注意：**注释夹在两个选择器片段之间会被浏览器拼成一个选择器**，导致整条规则静默失效。可运行 `scripts/check_css_selectors.py` 扫描该类问题。
- 显示 / 隐藏统一用项目的 `.hidden { display: none !important }` 类，**不要用 `hidden` 属性**（作者样式会盖掉它）。

### 7.3 数据库迁移

- 脚本放在 `server/src/main/resources/db/migration/`，命名 `V<序号>__<描述>.sql`，**只增不改**。
  **绝不修改已发布的迁移脚本** —— 项目开了 `validate-on-migrate`，改动历史脚本的 checksum 会让所有既有数据库启动失败。
- SQLite 改表能力很弱：
  - **只加列** → `ALTER TABLE ... ADD COLUMN`
  - **改 CHECK 约束 / NOT NULL / 改表名** → 只能走「建新表 → 搬数据 → `DROP` → `RENAME` → 重建索引」（V11 ~ V13 的 `memo` → `favorite` 改名就是这么做的）
- 软删除必须同时写入 `deleted_at`，恢复时清空。

---

## 8. 硬红线（违反会静默失效）

> 这一节是这个项目最贵的部分。下面每一条都对应一次真实故障，**共同特征是：不报错、不抛异常、日志干净、测试全绿，但功能就是不对**。
> 改代码前请至少扫一眼这一节。

### 8.1 删数据的「确认」必须与恢复能力对上

- **五类软删**（`inbox` / `task` / `event` / `favorite` / `knowledge`）→ 免二次确认，进回收站保留 30 天。**toast 文案必须含「回收站」**，让用户知道还能反悔。
- **硬删**（时间线删除、回收站的「彻底删除 / 清空」）→ **必须二次确认，且提示必须写明「无法恢复」**。
- 硬删白名单登记在静态检查脚本的 `HARD_DELETES` 里。**新增任何不可逆删除，必须同时登记进去**，否则会被当成软删检查、以「软删不需要二次确认」为由放过去。

### 8.2 接口写好没接上 = 功能不存在

新增后端接口 + `api.js` 方法之后，**必须确认 UI 真的在调它**。「后端有、前端有方法、就是没人调」这种半截功能在代码里完全看不出来。

### 8.3 静默失效三兄弟

| 写法 | 症状 |
|---|---|
| `$("id")` 撞到不存在的元素 | 顶层抛 `TypeError` → **整页脚本连同初始化一起死** |
| `handleAction` 缺处理分支 | 点了没反应，无任何提示 |
| 用 `hidden` **属性**隐藏元素 | 被作者样式盖掉 → 一直看得见；反之亦然 |

**可见性断言的正确写法**：用 `getComputedStyle` 读 `display` 并沿**祖先链**查 `display / visibility / opacity`，再配合 `offsetParent`。
查 `element.hidden` 会**恒为真**（断言假绿）—— 元素被容器用 `opacity: 0` 藏起来这种情况根本查不出来。

### 8.4 改前端样式必须量「最挤的那一档」

- 按钮尺寸 / 内边距 / `gap` / 字号 / placeholder 任何一个动了，都要扫一遍宽度。**三列布局最窄一档是 961px 视口**。
- 例：任务卡操作行原本 5 个按钮正好卡在 961px 的极限（可用 246px，实占 242px，余量 4px）。加第 6 个按钮溢出 43px 必然折行，最后靠把内边距从 10px 收到 6px、缝从 3px 收到 2px 才站得住。
- ⚠️ 这类收窄**必须带容器前缀**（`.task-card .tc-acts`），写成全站 `.btn.sm` 会波及收藏 / 日程 / 收录页。

### 8.5 ID 特异性主题块会连变体一起盖掉

`#view-tasks .btn { color: … }` 这类 ID 选择器（特异性 1,1,0）会盖掉 `.btn.primary`（0,2,0）。
曾经因此让主按钮文字对比度掉到 1.44:1，视觉上**等于没有文字**。
同理 `border-color` 简写会把 `border-left-color` 一起刷掉，曾导致三条泳道左侧细边同色。

**改完必须到真页面读计算样式（`getComputedStyle`）确认，不要只看截图。**

### 8.6 「遮挡」也是静默失效

展开 / 浮层 / 下钻 / 悬停这类改动，问题只有截图看得见 —— 元素在、样式对、`getBoundingClientRect` 正常，只是被另一个元素盖住了。
**含这四类行为的改动，必须截一眼。**

### 8.7 「重取数 + 重绘」≠「用户看得见」

三个列表都带**会收窄结果且页内一直保留**的条件：收藏的空间分组、任务的页内筛选与检索词、日程的周视图与检索。
新建一条记录后如果它被这些条件挡住，用户看到的现象是「怎么没反应」，而**前后端日志都没有任何异常**。

约定：四条创建路径（新建收藏 / 新建任务 / 新建日程 / 确认收录，另加批量确认与「移至」）都必须过**共用入口** `createdVisibilityNote()` ——
「取数后按 id 反查 → 不在视图里就把挡住它的条件放宽到一定看得见 → **放宽了什么必须说出来**」。
⚠️ 放宽后仍看不见（例如超出一次加载的前 100 条）**不许谎报**「已显示」，照实说并给出下一步。

### 8.8 共用 CSS 类的多个控件，选择器必须各带容器前缀

裸 `querySelector(".memo-switch")` 会取到第一个 → 事件委托绑错控件；
改用 `querySelectorAll` 又会把另一排一起清空。**两种都不报错。**
setter 要写成「非法值忽略 + 幂等守卫」。

### 8.9 兜底不能盖住更精确的错误

`/api/**` 上挂统一的 404 处理会吃掉 405。
另外：**MockMvc ≠ 真实容器** —— 路由注册、静态资源解析、错误页这些都以真实启起来的服务为准。

### 8.10 「含糊的界面抱怨」先量数据，别先改代码

「卡片太宽了」先量容器与内容宽度（曾把「全宽 1040px / 内容 250px」误判成布局问题，其实是内容太少）；
「文案不对」先枚举取值并统计长度分布。**先拿到数字，再动手。**

### 8.11 断言中文响应体必须显式指定编码

`getContentAsString()` 不传 `StandardCharsets.UTF_8` 会乱码，造成**假失败**。

### 8.12 验证脚本本身会骗你

- **脚本可能「静默只跑一半」**：一条 `const` 的暂时性死区抛 `ReferenceError`，会让整个脚本从那里退出，**后面一半断言一条都没跑**，而输出看起来只是「少了几条 OK」。
  → **跑满的标志是有收尾结果行**；断言数量变少先搜 `ReferenceError`。
- **变异测试的靶子必须是全库唯一的字符串**，且 `String.replace` **只替换第一处**——同名片段会换错地方，导致「变异没被抓住」的假绿。
- **宽度类变异的靶子必须打在「最挤的那一档」**：主视口开 1240px 时，「内边距改回 10px」本来就不折行，会报成假绿。
- **退出码不等于成败**：`git push` 的 stdout/stderr 可能被整个吞掉、退出码仍是 0。判定推送成功要对比 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/main`。
- **`git status --short --cached` 在部分 git 版本下返回空**，看着像「什么都没暂存」。判暂存请用 `git diff --cached --shortstat` + `git status --short`。

### 8.13 同一文件的多次编辑不要并行

对同一个文件在**一条消息里发多个 Edit**，两边都会回「成功」，但后写的会覆盖先写的，**改动可能静默丢失**。
改完**立刻用搜索复核关键行**，不要相信「Successfully edited」回执。不同文件之间并行是安全的。

### 8.14 判「有没有被别人改过」不要用字节数或 mtime

文件系统的 mtime / 大小在某些视图下有延迟。要查内容 —— 用关键标识的命中计数、或比对一段固定片段。

---

## 9. 测试与验证

### 9.1 测试

```bash
cd personal-workbench/server
./mvnw clean test
```

- 新增功能需附集成测试，参考已有的 `*FlowTest`（26 个测试类 / 265 个用例）。
- 修改接口契约时同步更新测试断言。

### 9.2 前端验证阶梯

改动越靠上，验证成本越低。**不要跳级**：

| 层级 | 手段 | 能发现什么 |
|---|---|---|
| 1 | 静态检查（`check_css_selectors.py` 等） | 选择器语法、只定义不调用的接口、危险的删除路径 |
| 2 | **截图** | 遮挡、错位、配色、折行 |
| 3 | **真实鼠标事件**（拖动、点击、悬停） | 事件委托绑错、状态机、防连点 |
| 4 | **真页面冒烟 + 在途变异** | 渲染链断裂、契约不一致、断言本身失效 |

- 冒烟脚本要在**真实运行的服务**上跑。用假后端（拦截请求就地造响应）时，天生看不见「真后端到底注册没注册这条路径」。
- 变异测试（故意把代码改坏，确认断言会红）是唯一能证明「断言真的在断言」的手段。**变异没被抓住，先怀疑断言写错了，而不是代码写对了。**

---

## 10. 打包与分发

打包的唯一入口是 `personal-workbench/deploy/build_packages.py`（纯标准库，无需 `pip install`）：

```bash
cd personal-workbench
python deploy/build_packages.py --version 1.0.4
```

一条命令产出两个 ZIP：

| 产物 | 用途 | 区别 |
|---|---|---|
| `personal-workbench-vX.Y.Z-clean-win.zip` | **发给别人** | `data/` 为空，首次启动进入配置向导 |
| `personal-workbench-vX.Y.Z-data-win.zip` | 自己换机 / 本机使用 | `data/` 内置真实数据快照 |

两者都是**自带裁剪 JRE 8 的 Windows 免安装包**，目标机解压双击即可。

### 脚本内置的 5 道硬校验

「宁可构建失败，也不产出坏包」：

1. **字节码校验** —— jar 主类必须是 major version 52（Java 8）
2. **前端新鲜度校验** —— 把 jar 内的 `static/` 与源码目录**逐字节比对**，任何不一致直接失败
3. **运行时校验** —— 自带 `java.exe` 必须存在且版本为 1.8
4. **干净包校验** —— 断言包内不存在任何 `*.db / *.db-wal / *.db-shm / *.log`，且文本不含 `sk-****`、`password_hash` 之类的凭据特征
5. **含数据包校验** —— `data/workbench.db` 必须存在且非空

其中第 2 条是本项目真实踩坑后加的：曾出现 jar 内 `app.js` 只有 41KB 而源码已 272KB、`poems.js` 完全缺失的情况 —— **jar 是半个月前构建的，差点把过期前端打进交付包**。

数据快照走 **SQLite `VACUUM INTO`**（等价于在线备份），**不是**直接复制 `workbench.db / -wal / -shm` 三件套 —— WAL 模式下后者可能得到不一致快照。

完整参数表、包结构与排错见 [`deploy/打包与分发说明.md`](personal-workbench/deploy/打包与分发说明.md)。

---

## 11. 分支模型与提交规范

### 分支模型

```text
main        ← 始终可发布，只接受来自 develop 的合并
  └── develop   ← 日常集成，功能完成后合入
        └── feature/xxx   ← 单个功能，从 develop 分出，完成后合并回 develop
        └── fix/xxx       ← 缺陷修复
```

- **不要**直接向 `main` 推送。
- 每个 PR 只做一件事，避免混杂无关的格式化改动。
- 分支命名：`feature/全局搜索语义联想`、`fix/收藏标题为空时回落正文`。

### 提交信息

采用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/)：

```text
<type>(<scope>): <简短描述>

<可选正文：为什么这么改>
```

| type | 用途 |
|---|---|
| `feat` | 新功能 |
| `fix` | 缺陷修复 |
| `refactor` | 重构（不改变行为） |
| `perf` | 性能优化 |
| `docs` | 仅文档 |
| `test` | 仅测试 |
| `chore` | 构建、依赖、工具链 |
| `style` | 格式调整（不影响语义） |

`scope` 建议使用后端模块名（`task` / `schedule` / `favorite` / `inbox` / `search` …）或 `ui` / `docs`。

示例：

```text
feat(schedule): 日程页改为周视图，支持手风琴单周展开
fix(search): 回收站命中不再跳转到错误页签
docs(readme): 补充免安装包解压目录的注意事项
```

### 提交前自查

- [ ] `git status` 中没有 `dev-data/`、`dist/`、`logs/`、`output/`、`*.db`
- [ ] `./mvnw clean test` 全绿
- [ ] 仅使用 Java 8 语法
- [ ] 新增数据库变更走新的 Flyway 版本号，未回改历史脚本
- [ ] 新增的不可逆删除已登记进 `HARD_DELETES`
- [ ] 新功能已补 `CHANGELOG.md` 条目

---

## 12. 提交 Pull Request

1. Fork 仓库，从 `develop` 切出功能分支。
2. 确保 `./mvnw clean test` 全绿。
3. 向 **`develop`** 分支发起 PR（不是 `main`）。
4. PR 描述中说明：**改了什么、为什么改、怎么验证**。
5. 关联相关 Issue（如 `Closes #12`）。

评审关注点：

- 是否违反了三条产品原则（尤其 **P1 零摩擦录入**）
- 是否引入了非必要的依赖
- 错误路径是否有可指导操作的提示
- 是否触碰了 [第 8 节](#8-硬红线违反会静默失效)的任何一条红线

---

## 13. 已知缺口

以下是明确知道、但尚未处理的问题，**改动相关模块时请留意**：

| 缺口 | 说明 |
|---|---|
| `confirmInboxItems` 未接入 UI | 后端接口已就绪，前端尚无入口。静态检查里唯一一条长期 FAIL 就是它 |
| 备份不含附件 | `BackupService` 只备份数据库，不含 `attachments/` 目录 → 「备份 → 删附件 → 恢复」会断链 |
| 附件物理回收调度器未实现 | 目前落盘文件只增不减，靠引用计数归零的逻辑留了接口 |
| 四份重复的 LLM 客户端 | `LlmClassifyProvider` / `SearchExpandClient` / `LlmAnswerClient` / `VisionProvider` 是四个几乎相同的 OpenAI 兼容客户端，重构另排期 |
| 死配置项 | `obsidian.enabled`、`ai.vision_enabled` 读的是配置但真正决定行为的另有其人（`isConfigured()` / `ai.vision_model` 非空），容易误导 |
| 干净机验收未做完 | 见 [`docs/干净机验收清单.md`](personal-workbench/docs/干净机验收清单.md) |

---

## 14. 报告问题

- 功能缺陷 / 功能建议 → 使用仓库里的 Issue 模板提交
- 安全漏洞 → **不要**开公开 Issue，请按 [SECURITY.md](SECURITY.md) 私密报告
