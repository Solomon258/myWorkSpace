# 安全策略

## 支持版本

| 版本 | 是否接受安全修复 |
|---|---|
| 1.0.x | ✅ |

## 报告安全漏洞

**请不要通过公开 Issue 报告安全漏洞。**

请使用 GitHub 的私密漏洞报告功能：

1. 打开仓库的 **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写复现步骤、影响范围与你的判断

或者直接访问：<https://github.com/Solomon258/myWorkSpace/security/advisories/new>

我们会在收到报告后尽快确认，并在修复完成后于 Release Notes 中致谢（如果你愿意署名）。

请在报告中尽量包含：

- 受影响的版本
- 复现步骤（能稳定复现的最小路径）
- 影响范围（能否读取/篡改本地数据、能否绕过登录口令）
- 你的初步判断与建议

## 设计上的安全边界

本项目是**纯本地、单用户**工具，理解这一点有助于判断问题是否属于安全缺陷：

| 项目 | 说明 |
|---|---|
| 网络暴露 | 服务默认仅监听本机 `localhost:18080`，不对外提供访问 |
| 账号体系 | 无多用户、无权限模型；登录口令用于防止同机他人误触，**不是**抗攻击的访问控制 |
| 数据存储 | SQLite 单文件存放于本地数据目录，**未做静态加密** |
| 外联行为 | 默认不联网；仅在启用 AI 整理 / AI 问答功能时会向所配置的服务地址发送待整理文本 |

因此「同机攻击者能读取未加密的本地数据库文件」属于已知的设计取舍，不作为漏洞受理；而「未配置 AI 却发生了外部网络请求」「未登录即可通过 HTTP 接口读写数据」则属于应当报告的安全缺陷。
