# 更新日志

本文件记录本项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 变更

- **日程新增表单按指定排版重排（三行骨架）**：① 重复 / 类型　② 日期 / 开始 / 结束　③ 左 ＋·附件、右 识别填表·加入。
  分钟快捷片 00/15/30/45 从时间框**下方**移到**上方**，与「日期」二字同处 24px 的「上排说明区」，
  下排控件框因此严格齐平（实测三个控件 top / bottom / width / height 跨度**全为 0**）。
  列宽取选定的**紧凑档**（三列各 200px、左对齐）。
  - `index.html` 的 `#composerBodyEvent` 换成 `.sf-grid > .sf-cell` 骨架；`style.css` 新增一段 `.sf-*`，
    **只新增、不动 `.c-row` 的老规则** —— 任务页与收藏页的 composer 还在用它，改它会连累那两处。
  - `.tp` 容器保留（`app.js` 靠 `input.closest(".tp")` 找 chips），样式上以 `display:contents` 摊平，
    让 `.tp-chips` 与 `input` 分别落进上、下两排 —— **`app.js` 一个字没改**。
  - 第三行的 `.media-gap` 从「＋ 与附件之间」移到「附件·问号 与 识别填表之间」：
    ＋·附件靠左、识别填表·加入靠右；四个控件统一 38px（原来媒体按钮 34px、主按钮 44px 是混着的）。
  - **第一行改为「类型 + 重复」**（用户要求「类型放在重复左边」）。
  - **第三行四个控件的高度 / 内边距 / 字号一次性对齐**（用户反馈「加入」与「识别填表」看着不一样高）。
    两者 height 本来就都由 `.btn` 规则锁成 38px，**真正不一样的是字号与内边距** ——
    `.media-attach` 自带 `padding:0 11px` 与自己的 font-size，`.btn` 走的是全局 `padding:8px 14px`，
    并排时外框等高、字面却不一样大。现在统一成 `padding:0 14px` + `font-size:13px`。
    ⚠️ 这条带 `.composer-body` 前缀，因此**任务页与收藏页的 composer 也一并统一**了
    （`probe_composers.js` 实测三个 composer 的媒体按钮现在全是 38px）；收录页用的是
    `.inbox-compose`，不在影响范围内。
  - 编辑弹层（`.editor-*` 命名空间）**未改动**，已加断言确认不受影响。
  - 验证：静态检查 **514 OK / 1 FAIL**（与改动前基线一致，唯一 FAIL 仍是长期缺口 `confirmInboxItems`）；
    真页面冒烟 `output/sched-layout/smoke_form.js` **45/45 绿、零 JS 异常**；在途变异 **6/6 被抓住、0 假绿**；
    影响面探针 `probe_composers.js` 确认任务 / 日程 / 收藏三个 composer 的媒体行尺寸一致。
- **「备忘」整体更名为「收藏」**：界面文案、前端标识符、后端包/类名/路由、数据库表与字段、注释与文档
  全部统一，不留一处旧名。
  - **命名映射**：`备忘`→`收藏`；包 `com.icecode.workbench.memo`→`.favorite`；类 `Memo*`→`Favorite*`；
    路由 `/api/v1/memos`→`/api/v1/favorites`；表 `memo`→`favorite`；索引 `idx_memo_*`→`idx_favorite_*`；
    约束 `chk_memo_*`/`fk_memo_inbox`→`chk_favorite_*`/`fk_favorite_inbox`；
    三处 CHECK 枚举值 `'memo'`→`'favorite'`（`activity_log.log_type` / `inbox_item.ai_category` /
    `attachment.owner_type`）；前端 `data-tab="memos"`→`"favorites"`、`#view-memos`→`#view-favorites`。
  - **数据库改名走 V11~V13 三条新迁移**，**V1~V10 一个字都没动**：
    Flyway 配了 `validate-on-migrate=true`，回头改已 apply 的迁移会让所有跑过旧版的库
    （本机、同事的包、分发包）在下次启动时集体卡在 checksum 校验、容器反复崩溃重启。
    代价是 V4 的文件名与 V1~V10 的历史注释里仍保留旧名 —— 那是历史记录，与「产品里叫法一致」不冲突。
  - **为什么是三条而不是一条**（这条踩过，记下来）：Flyway 8.5 会检查单个迁移内部的语句类型，
    只要同时出现「事务性」和「非事务性」语句就直接抛
    `FlywayException: Detected both transactional and non-transactional statements within the same
    migration (even though mixed is false)`。而 `PRAGMA` 的归类**并不统一** —— 实测
    `PRAGMA foreign_keys` 与 `PRAGMA legacy_alter_table` 放在同一个文件里**也会**被判成 mixed。
    所以拆成三条、**每条迁移只放一种语句类型**：`V11` 只放「关外键」一条，
    `V12` 放全部重建（纯 DDL/DML，**仍然原子、失败整体回滚**），`V13` 只放「开外键」一条。
    顺带证明了 Flyway 对 SQLite 的 DDL 是**走事务**的 —— 这正是要把数据手术单独留在 V12 的原因。
  - **只关外键就够，不需要 `legacy_alter_table`**：一度为了防 `ALTER TABLE ... RENAME` 重解析 schema
    报 `no such table: main.inbox_item` 而开过它，实测**不需要**（只关外键即可完整跑通），
    而且它正是撞上上面那条 mixed 限制的元凶，已删除。`SchemaMigrationTest` 里留了一条断言
    防它被重新加回来却忘了复位。
  - **为什么必须关外键**：`inbox_item` 被 5 张表用外键引用
    （`task` / `schedule_event` / `favorite` / `knowledge_note` / `wechat_msg_log`），
    而 `DROP TABLE` 在外键开启时会先做一次隐式 DELETE，子表还有行引用它时必然
    `FOREIGN KEY constraint failed`。**V13 的复位同样不能省** —— Flyway 的连接来自 Hikari 池，
    而 sqlite-jdbc 只在新建连接时施加 `enforceForeignKeys(true)`，还回池的连接不会自动纠正，
    漏了就等于整个运行时都在无外键保护下跑，且不报任何错。
  - **枚举值必须在搬迁那一刻就换掉**，不能等搬完再 `UPDATE`：新表的 CHECK 已经只认 `'favorite'`，
    原样搬 `'memo'` 的行会被当场拒掉（预演时正是被这条抓到）。
  - **AUTOINCREMENT 计数器要手工保住**：`DROP TABLE` 会连 `sqlite_sequence` 里的行一起删，
    重建后计数器回落到「搬过去的最大 id」。真实库里 `memo` 的 seq=17 而 max(id)=16（末尾删过一行），
    不保就会把已经进过回收站的 id 重新发出去。⚠️ 复原必须用 `UPDATE` 而非 `INSERT OR REPLACE` ——
    `sqlite_sequence` 的建表语句是 `CREATE TABLE sqlite_sequence(name,seq)`，**没有唯一约束**，
    `OR REPLACE` 不会覆盖而是插进第二条同名记录，普通 SELECT 取到的是旧的偏小值，看起来像「没生效」。
  - **三个编辑器共用的弹层骨架改成中性名**：`.memo-mask` / `.memo-modal` / `.mm-*` 的名字虽然沿自收藏，
    但 2026-09-17 起它同时服务收藏 / 任务 / 日程三个编辑弹层（`index.html` 里那段注释早已写明），
    再叫 memo 就是名不副实，故改为 `.editor-mask` / `.editor-modal` / `.em-*`。
  - **知识页的「收藏文章」保持原样不动**（用户明确选择）：那是另一条链路（`collect` 包 / 得到分享链接
    → Obsidian Vault + 知识库），与收藏页是两回事。同一应用里出现两个「收藏」是这次改名已知的代价，
    已在此记录，后续若要消除歧义可把知识页那一处改成「收录文章」。
  - 保留了三处**不能改**的同名物，均与功能无关：`javax.imageio.stream.MemoryCacheImageOutputStream`、
    `OutOfMemoryError`、以及文档里指第三方笔记应用的「备忘录」。
  - 验证：迁移在真库副本上预演通过（35 条结构/数据核对全绿）；**真实库上已实际应用 V11~V13 并核对 21 条**
    （表/索引/约束改名到位、三处枚举迁移、11 条收藏逐字段一致、自增计数器保住、
    `foreign_key_check` 干净、`integrity_check` = ok）；后端全量 **254 tests / 0 failures**；
    静态检查 **514 OK / 1 FAIL**（唯一 FAIL 仍是长期缺口 `confirmInboxItems`，与本次无关）；
    收藏页真页面冒烟 **38/38**，在途变异 **4/4 被抓住、0 假绿**。

- **任务页新增排序切换：按截止时间（默认）/ 按最后修改时间**（用户要求「任务菜单的所有任务应该提供两个排序」）：
  - 形态是**分段控件**（两档互斥、任何时刻都有一档选中），放在搜索卡片与看板之间。命名用
    `.task-sort` / `.ts-*`，**不复用**收藏页的 `.favorite-switch` / `.favorite-choice` ——
    复用类名就又是一个跨控件共享的选择器，而收藏页那边恰好有裸选择器（2026-09-14 收藏的「空间」
    切换器正是被复用同一个类名的任务切换器顶掉的，点了完全没反应）。
  - 默认档「按截止时间」与改动前**逐字一致**（泳道内先按优先级、再按截止日期，当天截止的置顶），
    请求里**不传** `sort`、URL 不变；选了「按最后修改时间」才带 `sort=updated`。
  - **排序落在服务端**（`GET /api/v1/tasks?sort=`），不是在渲染层本地重排：任务页一次只加载 100 条，
    本地重排只能在这 100 条里做，而「按最后修改时间」要看的恰恰是**最近改过的那一批** ——
    它们完全可能不在「按截止时间排出来的前 100 条」里。这与「检索写成本地过滤」是同一类静默失效。
  - 「按最后修改时间」**刻意不置顶当日截止**：用户选它就是为了看改动顺序，此时再按另一个维度
    把卡片拎到最前面，他会以为排序没生效（而界面上没有任何异常）。卡片上的「今天截止」高亮保留。
  - 后端 ORDER BY 用 `COALESCE(updated_at, created_at) DESC, id DESC`：`updated_at` 在 V1 里是可空列，
    直接排会让历史任务在 DESC 下全部沉到最后（等于静默踢出前 100 条）；`id` 兜底同一秒内改动的多条，
    保证顺序稳定、刷新不来回跳。非法 `sort` 报 400/1002 并写出合法取值，不静默回退成默认排序。
  - 点当前已生效的那一档是**幂等**的（不重取数、不弹提示）；两档的取数口径收敛到同一处
    （`refreshAll` 与 `reloadTasks` 都按 `taskSortMode()` 带参），避免「切完排序再搜索，顺序又变回去」。
  - **排序条左侧的「排序」二字已去掉，两个按钮左移顶格**（同日后续要求）：删掉 `index.html` 里的
    `<span class="ts-label">排序</span>` 与 `style.css` 里对应的那条规则（留着就是只定义不用的死规则）。
    分段控件本身就是「两个互斥档位」，选中态已经把语义说清楚了；那个前缀只会把按钮往右推、
    跟下方泳道看板的左边缘对不齐。改完 `.task-sort` 保持 `display:flex` 左起、无 `padding-left` /
    `margin-left`，`.ts-choices` 是它的第一个元素子节点，因此直接贴容器左边缘 ——
    **不是**用负外边距把位置硬拽回来的（那样在窄屏与不同字号下会溢出）。
  - 这一批是**纯位置改动，没有任何行为变化**：`app.js` 一个字没动（`data-action` / `data-sort`
    与 `applyTaskSort` 全不变），所以静态检查的取数接线断言与后端测试完全不受影响。
  - 验证：静态检查 **500 OK / 1 FAIL**（唯一 FAIL 仍是长期缺口 `confirmInboxItems`；此前记的
    476 是「下线今日主题」那一轮的基线，之后竖排诗词上线把 `poems.js` 也扫了进去、叠上本轮的排序断言）；
    静态层在途变异 **16/16 被抓住**；真页面冒烟 **28/28**（新增 6 条只在真浏览器里能量到的几何断言）；
    冒烟层变异 **7/7 + 静态资源变异 6/6**，均 0 假绿 0 崩溃。
  - **两个新踩到的坑，已写进断言与技能**：① 想断「HTML 里没有『排序』二字」时**不能扫全文** ——
    `index.html` 里那几个字满地都是，包括**有意写**在注释里的版式说明，扫全文会让「改注释 → 假红 →
    为了过检查去删注释」，反而丢掉线索；改成只扫 `#taskSortBar` 那一段（`.match` 切出来判）。
    ② 「顶格」不能只钉 `text-align`：`.task-sort` 是 `display:flex`，flex 容器里 `text-align`
    管不了主轴分布，用 `justify-content:center` 把按钮挪到中间时断言照样全绿 ——
    这条是**变异跑出来**的（改成同时盖 `justify-content` / `text-align` / `margin:auto` 才抓住）。

- **下线驾驶舱的「今日主题」**（用户反馈「没有什么用」）：按**彻底移除**处理，不是隐藏。
  - 前端：`index.html` 的整块主题卡片（输入框 + 「设定主题」按钮 + 问号气泡）、时间线问号气泡里
    「…番茄与今日主题都会在这里留下足迹」那句、`app.js` 的 `renderDashboard` 回填 / `saveTheme()` /
    **两处顶层监听**、`api.js` 的 `saveTheme`、`style.css` 的 `.theme-card` / `.theme-compose`
    （含 768px 媒体查询里 `.theme-compose` 那一半）。
  - 后端：`POST /api/v1/dashboard/theme`、`DashboardService.saveTheme` 与它注入的仓储、
    `DashboardTodayVO.theme`，并删除**只服务于这条链路**的 `ThemeRequest.java` 与 `DailyPlanRepository.java`
    （后者的 `insertActivity` 也只被 `saveTheme` 调过）。
  - **数据库与历史数据不动**：`daily_plan.theme` 列保留 —— 该表仍被 `daily_plan_item` 的外键依赖
    （回收站删任务要用它），删除列会波及那条链路；历史 `activity_log` 里的 plan 记录是用户真实流水，同样保留。
  - `PomodoroFlowTest` 里原来那个「设主题 → 幂等 → 回显」的用例改为
    `dashboardSummarizesInboxAndPomodoroWithoutTheme`：去掉主题断言，反向钉住
    「该接口已返回 404」+「响应里不再有 `theme` 字段」，防止它被悄悄加回来。
  - 静态检查基线仍是 **476 OK / 1 FAIL**（唯一 FAIL 是长期缺口 `confirmInboxItems`）；
    后端全量 **244 tests / 0 failures**；新增真页面冒烟 `output/dashboard-no-theme/smoke_dashboard.js`
    （**21 条断言 + 2 条在途变异**），基线全绿、变异 2/2 被抓住 ——
    其中 m1 复刻「只删 HTML 忘删 JS 里的取值」：顶层撞空元素抛错被 `initialize` 吞成 toast，
    且会让简报 / Top3 / 今天截止 / 今日日程**整段不渲染**；m2 把卡片加回来，布局断言从 14px 变 168px。

- **收藏的编辑区从「卡片内联面板」改成全局弹层**（用户反馈「文本框太小了，编辑起来很不方便」）：
  - 原来的编辑面板是**长在卡片内部**的 `.edit-panel`（`#favorite-edit-{id}`，靠 `classList.toggle("open")` 展开），
    宽度被 `.favorite-grid` 的列宽锁死：1120px 容器下三列、单卡约 353px，扣掉卡片左右 18px 与
    面板左右 15px 两级内边距后，正文 textarea 实际只剩 **约 285×80px**（`rows="3"`），
    而 `maxlength` 写的是 **4000**。**是面积不够，不是配色问题** —— 所以没有去调颜色，而是换了承载形态。
  - 现在正文实测 **672×312px**（1240×1000 视口），**面积约 9.2 倍**；640px 窄屏降级成全屏 sheet
    之后是 608×600。编辑区高度**不靠 JS 逐帧算**，而是让弹层自身 flex 撑满 ——
    好处有二：敲字时编辑区不会跟着抖动（`scrollHeight` 每敲一行就变一次），
    以及「还剩多少空间」交给浏览器算，窄屏 / 行高变化时不用再补一套 JS。
  - 弹层尺寸 `min(720px, 100vw - 40px)` × `min(560px, 100vh - 80px)`；标题 18px 无边框 + 底部分隔线；
    标签 + 空间沿用原编辑面板的 `1fr 118px` 比例；底栏左侧 `已输入 N / 4000`
    （`font-variant-numeric: tabular-nums`，防止数字位数变化时文字左右跳）。
    `Ctrl/Cmd + Enter` 保存、`Esc` 取消，快捷键提示常显在底栏。
  - **四个关闭出口（取消 / × / 点遮罩 / Esc）全部走同一个 `requestCloseFavoriteEditor`**，
    有未保存改动时先 `confirm` 二次确认。弹层是遮罩式的，误触关掉会连同刚敲的正文一起消失 ——
    这在本项目里等价于最忌讳的「静默无反应」。**保存失败时同样不关弹层**，理由一样。
  - **保存路径只有一条**：按钮点击与 `Ctrl+Enter` 都调到 `saveFavoriteEditor()`，
    两条路各写一份校验，早晚会出现「按钮能存的、快捷键存不了」。
  - ⚠️ **被清空的标题要原样发空串，不能转成 `null`**：后端 `FavoriteService.update` 是**局部更新**
    （字段为 `null` = 不动这个字段），发 `null` 会让「清空标题 → 回落到正文前 24 字」这条
    既有规则静默失效。已用冒烟断言钉住。
  - ⚠️ 弹层容器放在 `<main>` **之外**，且样式里不给它加 `transform` / `filter` / `will-change` ——
    这两条都是同一件事：它们会给 `position:fixed` 的后代**造出包含块**，弹层就被关进祖先里了，
    而控制台一个字都不报。
  - `Esc` 用**捕获阶段**挂并 `stopPropagation`：既有的两条 Esc 监听（收起页内搜索框、取消问号气泡固定）
    都在冒泡阶段，弹层开着时按 Esc 只应该关弹层，不该顺手把搜索框也收掉。
  - **收藏卡片模板里的内联面板整体删除**（每张卡少 5 个控件），
    任务页 `#edit-`、日程页 `#event-edit-` / `eventEditPanel` 的内联表单**原样保留**，一行没动。
    检查脚本里那批「第 4 个 `.edit-panel.me-form`」的断言已跟着迁移到弹层
    （`me-form` 4 → 3 处、`me-label` 18 → 14 个、`me-actions` 4 → 3 个、`cancel-favorite` 动作移除），
    并补了 `aria-label` / 弹层位置 / `position:fixed` / 正文 flex 四条新断言。

- **「解析图片（＋AI）」的 AI 角标由紫色改成「智驾蓝」，并补上灯感**：
  - 媒体行三个入口统一到一套新色（`:root` 的 `--ai-blue` 家族），**不再复用 `--purple` / `--blue`** ——
    `--blue` / `--blue-dark` 在任务页被 `#view-tasks` 换成了鼠尾草绿，复用会让同一个按钮在两个页签
    是两个颜色；`--purple` 还挂在时间线日期标签与引用来源上，改这个全局变量会连累两处无关的地方。
  - **「＋AI」与「识别填表」走浅蓝底**（`#EAF6FF` + `#A9DBFF` 描边 + `#0A76CC` 文字）；
    **「附件」仍是白底** + 同族描边与文字。二者职责不同（一个会调模型、一个只是带个文件），
    靠底色而不是靠位置区分。
  - **灯感**：＋按钮自己带一圈 3px 淡蓝光晕（hover 扩到 4px 并加深 = 点亮），
    AI 角标是 `1.5px 白描边 + 2.5px 淡蓝光晕` 两层（只有白描边会像印在按钮里，只有光晕会糊在一起）。
    `box-shadow` 不参与布局，所以按钮尺寸与媒体行高度都没变。
  - **角标底没有直接用最亮的 `#0F9BFF`**：9px 白字压上去只有 **2.68:1**（原紫是 6.93:1）会糊。
    拆成两层 —— 亮蓝只当「灯」（描边 / 图标 / 光晕），灯罩内壁压深到 `#0A76CC`（白字 **4.70:1**）。
  - 连带调到紫色的还有：拖拽落点高亮、缩略图描边、解析中的转圈与进度条。
    附件计数徽标与附件按钮 hover 的色值改成写死（原来走 `var(--surface2)` / `var(--blue-dark)`，
    在任务页会变成绿底绿字）。
  - ⚠️ 任务页主题块里补了 `#view-tasks .media-row .btn[data-media-extract]` 两条：
    `#view-tasks .btn` 是 ID 特异性（1,1,0），会压过媒体区的规则（0,3,1），
    不补的话「识别填表」在任务页会静默退回灰绿（已用变异回放验证）。

- **「新增」表单里的输入框，边框统一换成 ＋ 框的描边色（只换边框，内部仍是白底）**：
  - 覆盖四个新增入口：任务页 / 日程页 / 收藏页的「新建」表单，以及收录页那个一句话输入框
    （这四个容器都带 `data-media-scope`，一处选择器即可全覆盖）。
  - **只改 `border-color`**（→ `#A9DBFF`，即 ＋ 按钮的常态描边）。底色 / 字号 / 内边距 / 高度 / 圆角
    一律不动，所以控件尺寸与三列布局（最窄一档 961px 视口）都没有变化。
  - **聚焦态换成灯色 `#0F9BFF` + 一圈淡蓝聚焦环**，与常态的浅描边拉开层级 ——
    否则就成了「点了没反应」，是这个项目最忌讳的一类静默失效。
  - ⚠️ 任务页主题块里补了 `#view-tasks .composer-body input / textarea / select` 两条：
    `#view-tasks input…`（1,1,1）会压过 `.composer-body input…`（0,2,1），
    不补的话同一个「新建」表单在任务页是灰绿框、在日程页是浅蓝框（已用变异回放验证）。
  - `:not([type=checkbox]):not([type=radio])` 保留，但**它守的不是 `border-color`**：
    实测原生复选框是 `border-width:0px` + `border-style:none`，给它设颜色是空操作；
    连 `border-radius` 也拿不到（Chromium 对 `appearance:auto` 的复选框会屏蔽作者样式）。
    真正保护复选框的是既有的 `.composer-body .c-check input{width:auto;min-height:auto}`
    —— 没有它，输入框的 `min-height:38px` 会把复选框撑成整行宽。

- **任务页三条泳道的皮重做，对齐设计稿**（Ardot「看板任务管理界面」file `726794478956026`）：
  - **泳道从白底改成一块略深的浅色托盘**（`#edf1f6`，冷中性），白卡片浮在上面 ——
    卡片与泳道的分层从此由**底色差**给出；原先那层「泳道体顶部会化没的同色薄雾」随即下线
    （两层叠加会把白卡片的对比吃掉）。
    托盘色是拿 A/B 截图比出来的：任务页的 body 挂着偏蓝的背景图，用 shell 那支鼠尾草绿
    （`--surface2:#edf1ec`）泛黄、会和底图打架，见 `output/ui-preview-lanes/tray-{A,B,C}.png`。
  - **泳道头改成设计稿那样的平头**：去掉 3px 横向渐变顶条、去掉渐变底、去掉与卡片区的分隔线；
    状态圆点由 10px 圆角方块改成 **8px 正圆**；数量徽标由「白底 + 描边」改成**状态色浅底 + 状态色文字**。
    泳道名不再按泳道取色（三个名字同一个深色）—— 名字带状态色会让「待办」和「已完成」读起来像两个量级的标题。
  - **卡片**：圆角 11 → 12px、阴影改成设计稿那种更柔的一档（`0 4px 12px` / 6%）、
    描边淡化成 `#e8ecf0`；左侧 3px 细边保留（三条泳道共用同一个托盘色，这条边是卡片上
    唯一还能一眼看出「属于哪条泳道」的东西）。
  - **已完成 / 已取消的卡片在标题前加状态徽标**：绿圆白勾 / 灰圆白叉。
    它是**纯展示、不可点**的，与 2026-09-17 移除的那个圆形状态按钮不是一回事 ——
    状态流转仍然只能靠拖动与「取消 / 恢复」改。
  - **三条泳道的状态色换成设计稿的色相家族并整体压深一档**：
    待办 `#5b7290` 灰蓝 → 进行中 `#b45309` 琥珀 → 已完成 `#047857` 翡翠。
    压深的原因很具体：圆点这次压在**泳道托盘**上（不是压在 `accent-soft` 上），
    托盘比 soft 更深，`#F59E0B` / `#10B981` 这类 500 档原色会掉到 2.0 左右、不达标。
    复核脚本 `output/task-theme-lanes/contrast_lanes.py`，报告见同目录 `contrast-lanes.txt`。

- **三条泳道配色定稿：中性灰 / 青蓝 / 翡翠**（用户指定「进行中」用番茄钟那支蓝）：
  - **「进行中」改成番茄钟的蓝**：番茄钟的标识色是顶栏 `.pomo-chip` 那支青蓝 `#22d3ee`
    （色相 187.9°），但它对泳道托盘只有 **1.59:1**，直接当圆点色会糊在底上；
    因此取**同色相压深**到 `#0f8fb0`（对托盘 3.32:1），既留住「番茄钟的蓝」，又满足对比度。
    真页面冒烟里按**量出来的色相**钉住这条需求（H 差 4.4° ≤ 15°），不是钉一个十六进制串。
  - **「待办」从灰蓝改成无彩中性灰** `#7f8890`：这是被迫的，不是顺手换的 ——
    「进行中」占住蓝色之后，原来的待办 `#5b7290`（H 214°）与它只差 16°、Δ明度 15.7%，
    8px 圆点上会被读成同一个色，正是当天上午用户报过的那个问题。
    改灰后成为「无彩 → 高彩 → 深绿」三段（待办 vs 进行中的 Δ饱和 77%），
    也是 Trello / Jira / Linear 的经典看板惯例。
  - **明度要同时看两个量**：本版第一稿的待办 `#7b858c` 与进行中 `#0f8fb0` 的
    HSL 明度差 14.1%（看着足够），但**相对亮度完全相等**（都是 0.2288）——
    等于转成灰度后两只圆点一样亮。待办因此上调到 `#7f8890`（相对亮度 0.2413 > 0.2288）。
    复核脚本现在把「HSL 明度」与「相对亮度」两条单调性分别卡死，并新增两两区分度打分
    （Δ色相 / Δ明度 / Δ饱和 各 1 分，**至少 2 分**才合格）：本版 2/3 · 2/3 · 3/3。

- **任务页：移除「分组」，重做三条泳道的配色**（一次「减法 + 调优」）：
  - **移除分组的全部入口**：页顶的「分组 工作 / 生活 / 全部」切换器、卡片右上角的分组徽标、
    编辑表单里的「分组」下拉，以及 `state.taskGrp` / `setTaskGrp` / 取数层的 `grp` 参数全部下线，
    任务页一律展示全部任务（原来默认停在「工作」，等于默认就把列表静默收窄了一半）。
    **后端的 `grp` 字段、`V9` 迁移、关键词自动判定与 `POST /tasks/{id}/group` 接口都保留不动**
    （任务页只是不再有入口），`api.js` 的 `changeTaskGroup` 按既有约定标注为「前端不使用」。
  - **移除卡片左侧的圆形状态按钮**（原来点一下循环切换「待办 → 进行中 → 已完成」），
    卡片头部只剩「标题 + 优先级」两样。任务页里的状态流转改由**拖动卡片**完成
    （跨级拖动仍按状态机逐级走：`todo → doing → done`，后端会拒跨级）；
    「取消 / 恢复」按钮与驾驶舱 Top3 的「开始 / 完成」是不依赖拖动的另外两条路径。
  - **移除看板下方的操作说明**（「拖动卡片可在泳道之间移动…也可以在卡片左侧圆点上点一下循环切换」）
    —— 圆点按钮本身已经下线，再留着这句就是让用户去找一个不存在的东西；
    「怎么把卡片挪到别的泳道」改由空泳道里的占位文案就近演示。
  - **三条泳道改为一条从左到右的渐进式色彩带**，与各泳道的含义对齐：
    待办 `#5487ad` 蓝（未启动、冷静、最浅）→ 进行中 `#c07d16` 琥珀（活跃、最饱和、最醒目）
    → 已完成 `#227a4e` 绿（达成、最沉）。三组数字同时单调递进 —— 色相 206° → 36° → 150°、
    明度 50.4% → 42.0% → 30.6%、饱和 35.2% → 79.4% → 56.4%；首尾明度差 19.8%，
    修掉了用户早先报的「待办与已完成的圆点颜色相同，无法区分」（旧版明度差只有 5.1%）。
    每条泳道头部再压一条 3px 的**横向渐变顶条**（accent → accent-line），把「从左到右」在单条泳道内也画出来。
    对比度与区分度的复核脚本落在 `output/task-theme-d/contrast_push.py`，报告见同目录 `contrast-push.txt`。
  - **页面层次调优**：泳道体的顶部加一层会化没的同色薄雾（不然卡片和泳道底都是白的，只剩一圈描边在分层）、
    卡片左侧 3px 细边改用**所在泳道**的中间色（旧版是三条泳道共用的浅灰绿，等于没分色）、
    空泳道用**泳道同色**的虚线框画成「还空着」、备注改用与泳道同色的左侧细线单开一层、
    标题/元信息的字号层级重新排过。

- **「回收站」由文字页签改为页签栏右端的图标按钮**：回收站是低频、按需去翻的兜底入口，
  混在驾驶舱 / 任务 / 日程这些日常页签中间，等于把垃圾桶摆在书桌上，还长期占着导航条一个位置。
  现在它以一枚垃圾桶图标留在页签栏内、被顶到最右端，左侧七个日常页签（驾驶舱…知识）位置不变；
  图标与文字页签**同高、同色、同悬停底色、同选中底色**，所以看起来仍是页签栏的一员而不是外来控件；
  条目数仍以角标显示（贴按钮内右上角，不被页签栏的 `overflow:auto` 裁掉），
  键盘可 Tab 到并有焦点样式，进入回收站时图标转为选中态。
  页签行由 8 个文字页签减为 7 个 + 1 个图标入口。

- **常驻功能说明改为「问号图标 + 悬浮气泡」，36 处原生 `title` 一并接管**（用户要求）：
  - 新增三段样式：`.q`（问号图标）/ `.qt`（气泡）/ `.hrow`（同一行 flex 居中），只加不改。
    图标视觉 14px 描边圆、**热区 24×24**（触屏可达下限）；气泡固定 **320px** 宽、圆角 10px、
    内边距 10/12、距图标 8px、**0ms 出现** —— 刻意不写任何 transition，有过渡就等于有延迟。
    背景用 `var(--surface)`、文字 12.5px/行高 1.65（对比度不打折），描边 `#cfd9e6`、箭头用
    `::before` 的 rotate(45deg) 双边框做，不额外占一个元素。
  - **落位统一「方案 A · 同一行」**：标题与图标一起包进 `.hrow` 做 flex 居中。
    ⚠️ **不许用 `vertical-align` 数值矫正** —— 上一版 `vertical-align:-7px` 让图标视觉上沉到基线下方
    （用户原话「不要放在该行右下角」）。`.q` 上仍留 `vertical-align:middle`，那是给
    `<p class="c-hint">` 这类非 flex 上下文的兜底。
  - 贴容器右边缘的地方（搜索行、表单底栏行尾）必须加 `.qt.r` 右对齐，否则 320px 气泡被容器裁掉半截。
  - 交互：`:hover` 与 `:focus-visible` 显示；**点击固定**（触屏唯一入口）；Esc / 点空白关闭；
    **点气泡本体不关**（要能选中里面的文字）；同一时刻只固定一个。判断「在不在气泡内」走
    `event.composedPath()`（与全局搜索面板同一条理由：容器被同步重绘时 `closest()` 会失效）。
    监听器挂在 `document` 上做**事件委托** —— 所有视图都会被 `refreshAll` 整块重绘，逐个绑定必然丢。
  - **18 处常驻说明**收进气泡：驾驶舱今日主题、收录页 3 处、日程待定区、时间线、知识页 2 处、
    回收站 2 处（两段合成 1 条）、日程与收藏的折叠表单提示、设置页 5 处。
    其中 4 处超 90 字的长文改为「一句话标题 + `·` 明细」的分条气泡（明细单条控制在 20 字内）。
  - **36 处原生 `title` 的处置：挂问号 15 / 并进 placeholder 6 / 删 16 / 保留 4。**
    删的是与 `aria-label` 重复的（设置齿轮、回收站、番茄钟、全局搜索框）与 6 张指标卡的
    「点击后会去哪」（点完页面上有 `.filter-note` 写明筛选口径，再加 6 枚图标是纯噪音）；
    「解析图片」「识别填表」共 7 枚文案合并成**每个表单底栏 1 枚**，避免一行挂两个问号。
    保留的 4 处是 `#favoriteArchivedChip`（它说的是「这个列表被改过」＝状态可见性，藏起来就是静默失效）
    与收藏「全部 / 工作 / 生活」三枚（分段控件里加问号更糟，且 `.favorite-switch .favorite-choice small`
    本来就被 `display:none`，没有可见副标题可用）。
  - **刻意保留常驻的四类**（藏进问号等于制造静默失效）：`.filter-note` 筛选提示条、失败与错误提示、
    随输入收敛的状态提示（`confirm-pendinghint-*`）、危险操作说明；另加驾驶舱 hero 引导语、
    设置页 6 个卡片副标题，以及设置页那条「⚠️ 要填 API 模型名而不是产品名」——
    它是唯一一个「报错之后能自证」的说明，留在明面上比收起来划算。
  - 标题行加图标后逐处量过宽度：任务页搜索行（961px 最窄档）仍是一行高，表单底栏 6 个元素不换行。

- **收录页待确认卡片的三行常驻说明一并收进问号**（用户要求，接着上面那句继续）：
  卡片头 `[原文标题] [置信度胶囊] [?]`，气泡内容**按卡片条件拼装**：
  - 每张卡都有：「下面浅色区是 AI 给出的分类与字段，核对或修改后点「确认生成」」
  - 只有低于 70% 的卡才有：「这条低于 70%，不参与「一键批量确认」；沿用建议直接确认也可以」
  - 只有识别到重复的卡才有：「识别到「每周」：确认后会一次生成 N 期独立日程，之后可单独修改」
    （这串字样 **必须原样保留** —— `check_frontend_render.js` 第 769 行是对 app.js 做源码匹配的）
  - 删掉的：`.qc-cap`（「AI 建议整理为「X」，核对或修改后确认：」—— 它与下面「分类」下拉里的值完全重复）、
    `.task-meta.is-warn`（低置信度那句）、「识别到「每周」」那行。`.qc-cap` 的 CSS 规则一并删除。
  - **低置信度的视觉信号改由琥珀色 `.qc-flag` 独立承担** —— 那行说明字进气泡后，胶囊是唯一的常驻提示，
    所以它是「62%」而不是「62% + 一行琥珀字」。冒烟里专门加了一条变异守着它（把 `is-warn` 换成 `is-ok` 必须变红）。
  - ⚠️ **仍留在明文里的只有 `#confirm-pendinghint-*`**（「没选日期：确认后会先进入「待定时间」区」）——
    它是随日期输入实时收敛的状态提示，藏进问号就是静默失效；检查脚本也按 `class="task-meta hidden"` 逐字匹配它。
  - 卡片头那枚问号在卡片最右缘，气泡必须带 `.r` 右对齐，否则 320px 会被卡片裁掉（冒烟里有对应断言 + 变异）。
  - 随后用户又要求把「待确认」卡片头下那句「置信度低于 70% 的条目不参与「一键确认」，需要你逐条核对后点「确认生成」。」
    也收进同一枚气泡（整段 `<p class="muted">` 删除）。子弹顺序重排为「**先讲阈值规则 → 再讲宽松度 → 最后讲图片例外**」——
    原来阈值那句在明面上、另两条在气泡里，同一个知识被劈成两半，现在收口到一处，该卡片头**一行常驻说明都不剩**。

### 修复

- **Esc 关不掉已固定的气泡**：Chrome 的 `:focus-visible` 启发式是「最近一次交互若是键盘、
  则当前聚焦元素算 focus-visible」，而 Esc 本身就是键盘事件 —— 于是摘掉 `.pin` 之后
  `.q:focus-visible>.qt{display:block}` 又把气泡显示着，用户按 Esc 看到的是「按了没反应」。
  已改为 Esc 时对聚焦中的 `.q` 一并 `blur()`。**静态检查与渲染脚本都查不出这条，
  是真页面冒烟抓到的**（修前 26 条断言里它单独红）。

- **任务页主按钮的文案等于不可见**：「新建任务」与编辑面板里的「保存」都是深色字压在深绿底上
  （`#4e5a4c` on `#407a5e`，对比度 **1.44:1**）。原因是主题作用域里的 `#view-tasks .btn{color:#4e5a4c}`
  是 ID 特异性（1,1,0），压过了基类的 `.btn.primary{color:#fff}`（0,2,0），连带把「删除」的红字也一起盖掉。
  已用 `#view-tasks .btn.primary` / `.btn.danger` 补回。控制台不报错、结构断言也查不出，
  是无头截图核观感时发现的，现已补进静态断言与真页面冒烟。
- **卡片左侧细边的分色被静默抹掉**：`#view-tasks .task-card{border-color:…}` 同样是 ID 特异性，
  会连 `border-left` 一起刷成灰色，把基类里「左边框跟泳道走」的意图整个抹掉（实测三条泳道全是同一个色值）。
  已补 `border-left-color`，并在真页面冒烟里按泳道逐条比对计算样式。

- **知识库索引改为剪枝扫描 + 快照缓存**：此前每次请求都要遍历整个 Vault，且遍历是先枚举
  整棵树再做过滤。实测用户 Vault 有 8754 个条目、其中 90% 以上是无关内容（一个 Python
  虚拟环境 5665 个、`.git` 2129 个），真正要索引的 `.md` 只有 344 个；容器内一次遍历
  10.2 s、一次全量扫描 11.3 s，表现为「每次强刷打开知识库都要等十几秒」。
  现在隐藏目录在遍历时整棵剪掉（10.2 s → 0.6 s），索引快照在有效期内直接复用、并落盘
  供重启复用（热路径零磁盘访问）。
- **知识库索引状态可重试**：读取失败不再停在「正在读取 Vault 索引…」上，改为显示原因并给出
  「重试」入口；`index.html` 里那句写死的加载文案也一并去掉，避免每次开页先闪一遍。
- **收藏页「空间」切换器此前完全点不动**：新增任务分组切换器时，它为了复用样式也带上了
  `.favorite-switch` 类，而收藏那段的点击委托用裸的 `querySelector(".favorite-switch")` 取的是
  DOM 里更靠前的**任务**那排，导致收藏自己的「全部 / 工作 / 生活」一个监听都没挂上（点了没反应），
  任务那排却被绑了两次。现已把两段的选中态同步与点击委托各自限定到自己的容器
  （`.favorite-switch-wrap .favorite-switch …`；`2026-09-17` 任务页的分组切换器整体移除后，
  页面上只剩收藏这一处 `.favorite-switch`，前缀仍然保留 —— 它是本项目对共用类的约定），
  并给 setter 补上「非法值忽略 + 幂等守卫」，一次误调不再产生多余的整页请求。
  这类故障控制台不报错，只能靠「按钮点不动」发现。

### 计划中

- 版本 B：Docker Compose 部署形态，支持局域网与手机访问
- 知识库与 Obsidian 仓库的双向同步

---

## [1.0.0] - 2026-09-13

首个公开发布版本（版本 A：Windows 免安装包）。

### 新增

- **收集箱**：一句话即刻录入，本地规则或 LLM 自动整理为任务 / 日程 / 收藏 / 知识四类，低置信度不阻断人工确认
- **驾驶舱**：今日聚焦 Top 3 与六个可下钻指标，含今日番茄钟与每日计划
- **任务看板**：待办 / 进行中 / 已完成三泳道，拖动改状态，后端严格状态机校验
- **日程周视图**：下周一 / 本周 / 上周三段手风琴，任何时刻只展开一周；支持待定时间区与物化的重复日程
- **收藏**：工作与生活分组、主题配色、置顶与检索，标题与正文至少填写一项
- **知识库**：本地知识条目管理，支持对接 Obsidian 仓库与 AI 问答
- **全局搜索**：跨任务、日程、收藏、知识、收录五类实体及回收站，左列表右预览，含语义联想
- **番茄钟**：单次时长与休息可配置
- **回收站**：五类实体统一软删除，保留 30 天可恢复
- **时间线**：全量操作流水记录，物理删除需二次确认
- **跨实体搬运**：一条记录在四类实体之间转移，单事务完成「新建 + 软删 + 记流水」
- **备份与恢复**：每日自动备份，手动备份与连接池挂起式安全恢复
- **免安装交付**：自带 JRE 8 的 Windows ZIP 包，双击启动、首次自动建库

### 技术说明

- Java 8 + Spring Boot 2.7.18 + SQLite（HikariCP 连接池）+ Flyway（V1 - V8 迁移）
- 前端为零框架原生实现，无构建步骤
- Maven Wrapper 随仓库提供，使用者无需预装 Maven

---

[Unreleased]: https://github.com/Solomon258/myWorkSpace/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/Solomon258/myWorkSpace/releases/tag/v1.0.0
