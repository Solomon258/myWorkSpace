# 更新日志

本文件记录本项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **任务的工作 / 生活分组（默认「工作」）**：任务页顶部新增「分组 工作 / 生活 / 全部」切换器，
  **默认停在「工作」**——打开任务页第一屏就是工作事项，生活事项点一下就出来，不必每次自己筛。
  分组取值与备忘页的「空间」完全一致（`work` / `life`，配色也一致），两眼就知道是同一件事。
  筛选做在**取数层**（`GET /api/v1/tasks?grp=` 走 SQL），不是拿回 100 条再本地过滤：
  任务页一次只加载前 100 条，本地过滤会让「工作」视图少显示却看不出还有更多——这与既有
  `keyword` 检索是同一个坑，第二次踩，所以规则一致。
  分组判定复用 `MemoService.autoGroup()` 的关键词规则（**同一套规则的唯一实现**，不复制第二份），
  新建任务时若停在具体分组就以眼前这一侧为准、停在「全部」则按关键词自动判定；
  每条任务卡片上有一枚可点的分组徽章，一键翻到另一侧（改完若已不属于当前视图会明确提示，
  不让卡片凭空消失）。历史任务由迁移 `V9` 按同一套关键词回填（`山姆买梅花肉和牛肉` → 生活），
  **新列 `NOT NULL DEFAULT 'work'`**，保证默认视图里不会凭空少掉老任务。

### 变更

- **「回收站」由文字页签改为页签栏右端的图标按钮**：回收站是低频、按需去翻的兜底入口，
  混在驾驶舱 / 任务 / 日程这些日常页签中间，等于把垃圾桶摆在书桌上，还长期占着导航条一个位置。
  现在它以一枚垃圾桶图标留在页签栏内、被顶到最右端，左侧七个日常页签（驾驶舱…知识）位置不变；
  图标与文字页签**同高、同色、同悬停底色、同选中底色**，所以看起来仍是页签栏的一员而不是外来控件；
  条目数仍以角标显示（贴按钮内右上角，不被页签栏的 `overflow:auto` 裁掉），
  键盘可 Tab 到并有焦点样式，进入回收站时图标转为选中态。
  页签行由 8 个文字页签减为 7 个 + 1 个图标入口。

### 修复

- **知识库索引改为剪枝扫描 + 快照缓存**：此前每次请求都要遍历整个 Vault，且遍历是先枚举
  整棵树再做过滤。实测用户 Vault 有 8754 个条目、其中 90% 以上是无关内容（一个 Python
  虚拟环境 5665 个、`.git` 2129 个），真正要索引的 `.md` 只有 344 个；容器内一次遍历
  10.2 s、一次全量扫描 11.3 s，表现为「每次强刷打开知识库都要等十几秒」。
  现在隐藏目录在遍历时整棵剪掉（10.2 s → 0.6 s），索引快照在有效期内直接复用、并落盘
  供重启复用（热路径零磁盘访问）。
- **知识库索引状态可重试**：读取失败不再停在「正在读取 Vault 索引…」上，改为显示原因并给出
  「重试」入口；`index.html` 里那句写死的加载文案也一并去掉，避免每次开页先闪一遍。
- **备忘页「空间」切换器此前完全点不动**：新增任务分组切换器时，它为了复用样式也带上了
  `.memo-switch` 类，而备忘那段的点击委托用裸的 `querySelector(".memo-switch")` 取的是
  DOM 里更靠前的**任务**那排，导致备忘自己的「全部 / 工作 / 生活」一个监听都没挂上（点了没反应），
  任务那排却被绑了两次。现已把两段的选中态同步与点击委托各自限定到自己的容器
  （`.memo-switch:not(.task-switch)` / `.task-switch`），并给两个 setter 补上「非法值忽略 +
  幂等守卫」，一次误调不再产生多余的整页请求。这类故障控制台不报错，只能靠「按钮点不动」发现。

### 计划中

- 版本 B：Docker Compose 部署形态，支持局域网与手机访问
- 知识库与 Obsidian 仓库的双向同步

---

## [1.0.0] - 2026-09-13

首个公开发布版本（版本 A：Windows 免安装包）。

### 新增

- **收集箱**：一句话即刻录入，本地规则或 LLM 自动整理为任务 / 日程 / 备忘 / 知识四类，低置信度不阻断人工确认
- **驾驶舱**：今日聚焦 Top 3 与六个可下钻指标，含今日番茄钟与每日计划
- **任务看板**：待办 / 进行中 / 已完成三泳道，拖动改状态，后端严格状态机校验
- **日程周视图**：下周一 / 本周 / 上周三段手风琴，任何时刻只展开一周；支持待定时间区与物化的重复日程
- **备忘**：工作与生活分组、主题配色、置顶与检索，标题与正文至少填写一项
- **知识库**：本地知识条目管理，支持对接 Obsidian 仓库与 AI 问答
- **全局搜索**：跨任务、日程、备忘、知识、收录五类实体及回收站，左列表右预览，含语义联想
- **番茄钟**：单次时长与休息可配置
- **回收站**：五类实体统一软删除，保留 30 天可恢复
- **时间线**：全量操作流水记录，物理删除需二次确认
- **跨实体搬运**：一条记录在四类实体之间转移，单事务完成「新建 + 软删 + 记流水」
- **备份与恢复**：每日自动备份，手动备份与连接池挂起式安全恢复
- **免安装交付**：自带 JRE 8 的 Windows ZIP 包，双击启动、首次自动建库

### 技术说明

- Java 8 + Spring Boot 2.7.18 + SQLite（HikariCP 连接池）+ Flyway（V1 - V8 迁移）
- 前端为零框架原生实现，无构建步骤
- Maven Wrapper 随仓库提供，使用者无需预装 Maven

---

[Unreleased]: https://github.com/Solomon258/myWorkSpace/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/Solomon258/myWorkSpace/releases/tag/v1.0.0
